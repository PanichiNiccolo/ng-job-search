import { StarHighlightDirective } from './star-highlight.directive';

describe('StarHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new StarHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
